import React from 'react'
import './Card.scss'

export default function Card() {
    return (
        <div className="card">
            <h4>Project</h4>
            <p>This is the project that we are working on right now.</p>
        </div>
    )
}
